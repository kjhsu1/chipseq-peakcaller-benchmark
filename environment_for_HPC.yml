name: background_project
channels:
  - conda-forge
  - bioconda
channel_priority: strict

dependencies:
  # ---- Core / solver ----
  - python=3.10
  - mamba
  - pip
  - wheel
  - cython<3

  # ---- Scientific stack ----
  - numpy
  - scipy
  - pandas
  - natsort

  # ---- Bioinformatics tools ----
  - bowtie2=2.5.*
  - bwa=0.7.17
  - samtools=1.19.*
  - pysam=0.22.*

  # ---- Workflow / ML ----
  - snakemake-minimal=7.32.4
  - hydra-core>=1.3,<1.4
  - mlflow>=2.10,<3

  # ---- Peak callers (bioconda builds) ----
  - macs2=2.2.7.1
  - epic2=0.0.52

  # ---- pip (keep minimal; only what’s not in conda) ----
  # (empty on purpose)
